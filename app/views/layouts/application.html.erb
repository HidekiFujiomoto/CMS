<!DOCTYPE html>
<html>
  <head>
    <title>CommunityManagementSystem</title>
    <%= csrf_meta_tags %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <div class="container">
      <div class="error_msg">
        <% flash.each do |key, value| %>
          <%= content_tag(:div, value, class: "#{key}") %>
        <% end %>
      </div>

      <div class="login_msg">
        <% if logged_in? %>
          <p class="text_right"><%= link_to "Logout", session_path(current_user.id), method: :delete %></p>
        <% else %>
          <p class="text_left"><%= link_to "ログインしてください", new_user_path %></p>
        <% end %>
      </div>

      <%= yield %>

    </div> <!--- /container --->

  </body>
</html>
